/*
package cn.code.chameleon.quartz.factory;

import cn.code.chameleon.pojo.ChameleonTask;
import cn.code.chameleon.quartz.utils.TaskUtils;
import org.apache.log4j.Logger;
import org.quartz.Job;
import org.quartz.JobExecutionContext;
import org.quartz.JobExecutionException;

*/
/**
 * 
 *
 *//*

public class QuartzJobFactory implements Job {
	public final Logger log = Logger.getLogger(this.getClass());

	@Override
	public void execute(JobExecutionContext context) throws JobExecutionException {
		ChameleonTask scheduleJob = (ChameleonTask) context.getMergedJobDataMap().get("scheduleJob");
		TaskUtils.invokMethod(scheduleJob);
	}
}*/
